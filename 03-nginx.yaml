- name: "installing nginx"
  hosts: web
  tasks:
    - name:
      ansible.builtin.package:
        name: nginx
        state: present
    
    - name:
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes