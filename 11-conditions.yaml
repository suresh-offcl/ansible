- name: 
  hosts: web
  tasks:
  - name:
    ansible.builtin.command: id expense
    register: user
    ignore_errors: true
  
  - name:
    ansible.builtin.command: useradd expense
    when: user.rc != 0
  
  - name:
    ansible.builtin.debug: 
    msg: "{{ user }}"
















  
